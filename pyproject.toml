[project]
name = "bridge-brief"
version = "0.1.0"
description = "Evidence-linked infrastructure inspection brief generator"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
imagery = ["Pillow>=10.0"]
llm = ["anthropic>=0.40"]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
